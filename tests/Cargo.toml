[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
libraw_r = { path = "../libraw_r" }

[dev-dependencies]
libraw_r = { path = "../libraw_r" }
criterion = { version = "0.4.0", features = ["html_reports"] }

[[bench]]
name = "libraw"
harness = false
