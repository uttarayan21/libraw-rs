[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
libraw_r = { path = ".." }

[dev-dependencies]
libraw_r = { path = ".." }
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "libraw"
harness = false
