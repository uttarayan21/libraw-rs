[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
libraw_r = { path = "../libraw-rs/" }

[dev-dependencies]
libraw_r = { path = "../libraw-rs/" }
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "libraw"
harness = false
